### 디렉터리 구조

`__init__.py` : 패키지 초기화 파일입니다. 이 파일이 존재하면 해당 디렉터리를 파이썬 패키지로 인식합니다.



RESTAPI 정의

- GET /tasks
- POST /tasks
- PUT /tasks/{task_id}
- DELETE /tasks/{task_id}
- PUT /task/{task_id}/done
- DELETE /tasks/{task_id}/done

#### uv 환경 설정

1. uv init
2. 